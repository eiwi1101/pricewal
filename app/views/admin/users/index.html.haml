%h1 All Users
%table
  %tr
    %th Username
    %th Email
    %th Admin
    %th Notify
    %th

  -@users.each do |u|
    %tr
      %td=u.username
      %td=u.email
      %td{align: 'center'}=u.admin? ? "<i class='fa fa-check'></i>".html_safe : ""
      %td{align: 'center'}=u.notify? ? "<i class='fa fa-check'></i>".html_safe : ""
      %td{align: 'right'}
        =link_to edit_admin_user_path(u) do
          %i.fa.fa-pencil

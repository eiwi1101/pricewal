<div id="flashes">
<% flash.each do |key, val| %>
  <div class='<%= key %>'><%= val %></div>
<% end %>
</div>

<%- if content_for? :toolbar %>
<div id="toolbar">
  <%= content_for :toolbar %>
</div>
<% end %>
  
<div id="content">
  <%= yield %>
</div>
